// <!-- 貓貓貓貓頭鷹啦 -->
// <script src="OwlCarousel/owl.carousel.min.js"></script>
$('.owl-carousel').owlCarousel({
	items:1,
	autoHeight:true,
	// loop:true,
	// margin:10,
	autoWidth:true,
	items:4
});
AutoHeight.Defaults = {
	autoHeight: false,
	autoHeightClass: 'owl-height',
};